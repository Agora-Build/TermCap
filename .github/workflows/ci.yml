name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-14]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v5

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      # The end-to-end suite drives real shells under tmux. zsh is installed
      # explicitly so the zsh integration is covered on Linux too; without it
      # the runner has only bash and half the shell code goes untested.
      - name: Install tmux and zsh
        run: |
          if [ "$RUNNER_OS" = "Linux" ]; then
            sudo apt-get update && sudo apt-get install -y tmux zsh
          else
            brew install tmux || true
          fi
          tmux -V && zsh --version

      - name: Format
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --all-targets -- -D warnings

      - name: Test
        run: cargo test --all-targets

      # Guards the claim in the README that these are the supported extents.
      - name: Smoke test the CLI
        run: |
          cargo build
          ./target/debug/tcap --help
          ./target/debug/tcap init zsh > /dev/null
          ./target/debug/tcap init bash > /dev/null
          ./target/debug/tcap init fish > /dev/null
          # Outside any supported terminal this must fail with guidance, not panic.
          if ./target/debug/tcap 2>err.txt; then
            echo "expected failure outside a supported terminal" && exit 1
          fi
          grep -q "tcap doctor" err.txt || { echo "error text lost its guidance:"; cat err.txt; exit 1; }
